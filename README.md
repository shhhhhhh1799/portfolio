# 🔧 Verilog 임베디드 통합 시스템 프로젝트

본 프로젝트는 **Verilog HDL**을 사용하여 구현된 임베디드 시스템으로, **디지털 회로 설계**, **센서 데이터 처리**, **UART 통신**, **FND(7-Segment) 디스플레이 제어**를 포함한 통합 시스템입니다.

총 세 가지 주요 기능을 FPGA 상에서 구현하였으며, 버튼 및 스위치를 이용한 조작과 센서 기반 입력, UART 통신 기반 출력까지 포함되어 있습니다.

## 🧑‍💻 팀 정보

- **팀명**: Team7
- **팀원**: 박승헌, 서윤철, 권희식, 오정일
- **진행 기간**: 2025.05.19 ~ 2025.06.02
- **역할**:
  - DHT11 온습도 센서: 박승헌
  - UART 통신: 서윤철
  - SR04 초음파 센서: 권희식
  - WATCH_STOPWATCH: 오정일

---

## 📁 프로젝트 구성

| 시스템 이름         | 설명                                                         |
|---------------------|--------------------------------------------------------------|
| ⏱ `Top.v`           | Stopwatch (스톱워치) + Watch (시계) 기능, FND 표시           |
| 📏 `top_sr04_uart`   | HC-SR04 초음파 센서를 이용한 거리 측정 시스템               |
| 🌡️ `top_dht11_uart` | DHT11 온습도 센서 기반의 데이터 수집 및 UART 송신 시스템    |

---

## 1️⃣ ⏱ Stopwatch / Watch 시스템 (`Top.v`)

### ✅ 주요 기능
- `sw[1]` 스위치를 통해 **스톱워치**와 **시계** 모드 전환
- 시계 모드에서 `btnL`, `btnR`로 시/분/초 설정 항목 선택 가능
- 설정 항목 선택 후 `btnU`, `btnD`로 시간 조정 가능
- FND를 통해 시간 출력 (시:분 또는 분:초)

### ⌨️ 입력 핀 설명
| 입력       | 설명                                    |
|------------|-----------------------------------------|
| `clk`      | 시스템 클록 (예: 100MHz)                |
| `rst`      | 전체 시스템 리셋                         |
| `btnL`     | 스톱워치: Clear, 시계: 항목 왼쪽 이동    |
| `btnR`     | 스톱워치: Run/Stop, 시계: 항목 오른쪽 이동 |
| `btnU/D`   | 시계 모드에서 값 증가/감소               |
| `sw[1:0]`  | sw[1]: 모드 선택, sw[0]: 표시 모드 선택  |

### 💡 출력 핀 설명
| 출력       | 설명                                      |
|------------|-------------------------------------------|
| `fnd_data` | 7-Segment (8비트) 표시 데이터              |
| `fnd_com`  | FND 자리 선택 (4비트 공통 단자 제어)       |
| `led[3:0]` | 현재 모드 또는 설정 상태 표시              |

### 🧪 예시 시나리오
- `sw[1]=1`: 스톱워치 모드
  - `btnR`: 시작/정지
  - `btnL`: 시간 초기화
- `sw[1]=0`: 시계 모드
  - `btnL/btnR`: 시/분/초 항목 선택
  - `btnU/btnD`: 시간 설정 (증가/감소)

---

## 2️⃣ 📏 초음파 거리 측정 시스템 (`top_sr04_uart`)

### ✅ 주요 기능
- HC-SR04 초음파 센서를 이용한 거리 측정
- 측정된 값을 FND로 출력 + UART로 전송
- 버튼을 눌러 측정 시작 가능

### 📦 하드웨어 구성
- 초음파 센서: **HC-SR04**
- 입력: `btn_start`, `echo`
- 출력: `trig`, `tx`, `fnd_data`, `fnd_com`

### ⌨️ 입력 및 출력 설명
| 핀 이름     | 설명                              |
|------------|-----------------------------------|
| `btn_start`| 측정 시작 버튼                     |
| `echo`     | 초음파 센서 echo 신호 입력         |
| `trig`     | 초음파 센서 trigger 신호 출력      |
| `fnd_data` | 거리 값 표시용 FND 데이터 출력     |
| `tx`       | UART 송신선                        |
| `rx`       | UART 수신선 (미사용 시 GND 연결)   |

### 🧪 UART 출력 예시
거리(cm): 1.23

---

## 3️⃣ 🌡️ DHT11 온습도 측정 시스템 (`top_dht11_uart`)

### ✅ 주요 기능
- UART 수신으로 `'s'` 문자 받으면 측정 시작
- DHT11 센서에서 온도 / 습도 수집
- UART로 메시지 전송 + FND로 표시
- 센서 통신은 10us 기반의 FSM으로 설계

### 📦 하드웨어 구성
- 센서: **DHT11**
- UART: 송수신 모두 사용
- 출력: FND (온도 2자리 + 습도 2자리)

### ⌨️ 핀 구성
| 핀 이름      | 설명                               |
|-------------|------------------------------------|
| `rx`        | UART 수신 (`'s'` 신호)              |
| `tx`        | UART 송신                           |
| `dht11_io`  | DHT11 양방향 데이터 핀              |
| `fnd_data`  | FND 출력                            |
| `LED`       | 측정 유효 시 점등                   |
| `state_led` | FSM 상태 디버깅용 LED               |

### 🧪 UART 출력 메시지 예시
Temp = 25C
Humi = 43%

---

## 🛠 공통 모듈 설명

| 모듈 이름         | 기능 설명                                         |
|------------------|--------------------------------------------------|
| `btn_debounce`   | 버튼 디바운싱 + 엣지 검출                         |
| `digit_splitter` | 시간/거리/온도/습도를 자리수로 분리               |
| `fnd_controller` | FND 자리 스캔 제어 + 데이터 선택                  |
| `uart_controller`| FIFO 기반 UART 송수신 (Tx + Rx) 제어              |
| `hex_to_ascii`   | 거리/온습도 숫자 → ASCII 코드로 변환              |
| `dht11_controller`| FSM 기반 DHT11 통신 (10us tick, valid 처리 포함) |

---

## 🔄 빌드 및 실행 방법

### ✅ Vivado 환경에서 Top Module 설정
- `Top.v`: Stopwatch / Watch 모드용
- `top_sr04_uart.v`: 거리 측정 시스템용
- `top_dht11_uart.v`: 온습도 측정용

### 🧪 시나리오 예시
| 사용 장면           | 조작 방법 / 흐름                               |
|---------------------|-----------------------------------------------|
| 시계 설정           | `sw[1]=0`, `btnL/btnR`: 항목 선택, `btnU/D`: 값 변경 |
| 스톱워치            | `sw[1]=1`, `btnR`: 시작/정지, `btnL`: 초기화  |
| 초음파 거리 측정     | `btn_start` → FND + UART 출력                  |
| DHT11 온습도 측정    | UART로 's' 입력 → 측정 시작 → 결과 표시        |
